/*
import React from 'react';
import Thumbnail from "./Thumbnail/Thumbnail.jsx";

const Thumbnails = ({images, setPosition}) => {
    return (
        <div>
            <h1>Thumbnails</h1>
            {
                images.map((img,index) => (
                    <Thumbnail key={index} index={index} img={img} setPosition={setPosition}/>
                ))
            }
        </div>
    );
};

export default Thumbnails;*/
